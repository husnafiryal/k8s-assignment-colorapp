apiVersion: v1
kind: Service
metadata:
  name: una-assignment-service
spec:
  type: ClusterIP
  selector:
    app: una-assignment-app
  ports:
  - protocol: TCP
    port: 80         # Port service eksternal internal-cluster
    targetPort: 5000 # Menuju port container aplikasi
