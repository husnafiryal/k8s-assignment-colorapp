apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: una-ingress
spec:
  rules:
  - host: una.apps.k3s.zauqilab.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: una-service
            port:
              number: 80
